name: Play the game
on:
  issues:
    types: [opened]
jobs:
  playGame:
    runs-on: unbuntu-latest
    steps:
      - name: see event id
        run: echo $EVENT_ID
        env:
          EVENT_ID: ${{ github.event.issue.id }}
